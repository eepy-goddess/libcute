# If true, it builds libcute as a dynamic library. If false, it builds libcute as a static library
IS_DYNAMIC = true

# If this option is set to false, libcute's header files won't be installed into the system header directory
INSTALL_HEADERS = true


